# API gateway (Phase 5): single entry point on :8080 that routes to the
# services. Runs in Docker; the Node/Vite services run on the host during
# dev (they shell out to Windows-native whisper/ffmpeg binaries, which is
# why they aren't containerized yet), reached via host.docker.internal.
events {}

http {
  # Long timeouts: live transcription passes and Excel generation can take
  # several seconds; stop-with-audio uploads can be large.
  proxy_read_timeout 120s;
  proxy_send_timeout 120s;
  client_max_body_size 120m;

  # Only send "Connection: upgrade" when the client actually asked for a
  # websocket (Vite HMR); sending it on plain requests breaks proxying.
  map $http_upgrade $connection_upgrade {
    default upgrade;
    "" "";
  }

  server {
    listen 8080;

    # API service (auth, patients, appointments, conversations)
    location /api/ {
      proxy_pass http://host.docker.internal:5000;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # Frontend (Vite dev server; a production build would serve static files)
    location / {
      proxy_pass http://host.docker.internal:5173;
      proxy_set_header Host $host;
      proxy_http_version 1.1;
      proxy_set_header Upgrade $http_upgrade;      # Vite HMR websocket
      proxy_set_header Connection $connection_upgrade;
    }
  }
}
